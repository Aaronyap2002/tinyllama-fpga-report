% Chapter4/chapter4.tex -- Implementation
% -----------------------------------------------------------------------------
% Chapter 4 takes a bottom-up approach: each section covers one computational
% primitive, discussing its implementation and pragma strategy before moving
% to the next. The top-level forward function is covered last, once all
% building blocks have been established.
%
% Section structure:
%   4.1  RMSNorm
%   4.2  Quantisation and Dequantisation
%   4.3  Matrix--Vector Multiplication
%   4.4  Softmax
%   4.5  Grouped-Query Attention
%   4.6  SwiGLU Feed-Forward Network
%   4.7  Top-Level Forward Function

\chapter{Implementation}
\label{ch:implementation}

All kernels in this chapter are implemented in Vitis HLS 2021.2,
targeting the \texttt{xczu9eg-ffvb1156-2-e} device on the ZCU102 evaluation
board at a design clock of \SI{150}{\mega\hertz}. Each primitive is first
presented as a standalone synthesisable function, allowing its resource
utilisation and initiation interval to be measured in isolation before it
is composed into the full transformer forward pass.

Development proceeded across two configurations. A scaled-down prototype
configuration was used to establish the kernel structure, verify the HLS
build flow, and confirm functional correctness in C-simulation without the
lengthy synthesis times and large on-chip resource requirements of the full
model. Once each component was validated at small scale, the configuration
constants were updated to target the full TinyLlama-1.1B dimensions.
Table~\ref{tab:config_comparison} summarises the two sets of parameters.

\begin{table}[H]
  \centering
  \begin{tabular}{lrr}
    \toprule
    Parameter & Prototype & TinyLlama-1.1B \\
    \midrule
    Hidden dimension $d_{\text{model}}$ & 8       & 2{,}048  \\
    FFN hidden dimension $d_{\text{ff}}$ & 32     & 5{,}632  \\
    Number of layers $L$                 & 2       & 22       \\
    Query heads $H$                      & 2       & 32       \\
    KV heads $G$                         & 2       & 4        \\
    Vocabulary size $V$                  & 32      & 32{,}000 \\
    Maximum sequence length              & 16      & 2{,}048  \\
    Quantisation group size $GS$         & 4       & 64       \\
    \bottomrule
  \end{tabular}
  \caption{Prototype and full TinyLlama-1.1B configuration parameters
           used across kernel development.}
  \label{tab:config_comparison}

\end{table}

The prototype dimensions were chosen deliberately to keep array sizes small
enough that HLS C-simulation completes in seconds and synthesis reports are
straightforward to inspect. With $d_{\text{model}} = 8$ and $GS = 4$, for
instance, every quantised vector contains exactly two groups, making
scale-factor indexing trivial to verify by hand. The constraint that $GS
\leq d_{\text{model}}$ must always hold, since quantising into groups larger
than the vector itself is undefined. At full scale the group size grows to
$GS = 64$, which divides evenly into all relevant dimensions in TinyLlama
(2048, 5632, 256) and aligns with a typical cache line size of 64 bytes,
making group-boundary loads naturally efficient.

% =============================================================================
\section{RMSNorm}
\label{sec:rmsnorm_impl}
% =============================================================================

\subsection{Implementation}
\label{subsec:rmsnorm_impl}

The \texttt{rmsnorm<S>} function is declared as a C++ template in
\texttt{forward.h}, parameterised by the vector length \texttt{S}. This
makes the same source compile correctly for both the prototype configuration
($S = d_{\text{model}} = 8$) and the full TinyLlama model
($S = d_{\text{model}} = 2048$) without any code changes. The function
takes three pointer arguments: an output array \texttt{o}, an input
activation array \texttt{x}, and a learned element-wise weight array
\texttt{weight}, all of length \texttt{S}.

The first design decision is to copy both \texttt{x} and \texttt{weight}
into local arrays \texttt{x\_buff} and \texttt{weight\_buff} before any
computation begins, as shown in Listing~\ref{lst:rmsnorm}. This is
necessary because Vitis HLS can only apply \texttt{ARRAY\_PARTITION} to
arrays whose storage it controls. Arrays received as pointer arguments may
alias external memory, which prevents the tool from freely splitting them
across multiple BRAM banks. By staging the data into local buffers first,
the \texttt{ARRAY\_PARTITION cyclic factor=4} pragma can be applied,
enabling four simultaneous reads per cycle in both computation loops.

\begin{lstlisting}[
  caption = {\texttt{rmsnorm<S>} implementation from \texttt{forward.h}.
             Data is staged into local buffers before computation so that
             \texttt{ARRAY\_PARTITION} can be applied. The reduction loop
             uses \texttt{UNROLL factor=4} to accumulate four partial sums
             simultaneously; the normalisation loop uses \texttt{UNROLL
             factor=64} since it is fully data-parallel.},
  label   = {lst:rmsnorm}
]
template <int S>
void rmsnorm(float o[S], float x[S], float weight[S])
{
    float x_buff[S], weight_buff[S], out_buff[S];
    #pragma HLS array_partition variable=x_buff      cyclic factor=4
    #pragma HLS array_partition variable=weight_buff cyclic factor=4
    #pragma HLS array_partition variable=out_buff    cyclic factor=4

    memcpy(x_buff,      x,      S * sizeof(float));
    memcpy(weight_buff, weight, S * sizeof(float));

    float ss = 0.0f;
sum_of_squares:
    for (int j = 0; j < S; j++) {
        #pragma HLS PIPELINE
        #pragma HLS UNROLL factor=4 skip_exit_check
        ss += x_buff[j] * x_buff[j];
    }
    ss /= S;
    ss += 1e-5f;
    ss = 1.0f / sqrtf(ss);   // reciprocal RMS

norm_and_scale:
    for (int j = 0; j < S; j++) {
        #pragma HLS PIPELINE
        #pragma HLS UNROLL factor=64
        out_buff[j] = weight_buff[j] * (ss * x_buff[j]);
    }
    memcpy(o, out_buff, S * sizeof(float));
}
\end{lstlisting}

The computation proceeds in two sequential loops with different parallelism
requirements. The first loop, \texttt{sum\_of\_squares}, accumulates the
sum of squared activations. This is a reduction with a loop-carried
dependency on the accumulator \texttt{ss}: each iteration reads the
current value of \texttt{ss}, adds to it, and writes it back. This
dependency means the loop cannot be pipelined to II\,=\,1 on its own
because the write of iteration $t$ must complete before iteration $t+1$
can read. The \texttt{UNROLL factor=4} directive resolves this by
unrolling four iterations into the same pipeline stage: four independent
partial products $x_j^2, x_{j+1}^2, x_{j+2}^2, x_{j+3}^2$ are computed
in parallel and their sum is added to \texttt{ss} once per group, reducing
the effective number of dependent additions by a factor of four.

Once \texttt{ss} has been computed, the scalar reciprocal RMS is available
and the second loop, \texttt{norm\_and\_scale}, has no loop-carried
dependencies. Every iteration reads one element of \texttt{x\_buff} and
one element of \texttt{weight\_buff}, multiplies by the scalar \texttt{ss},
and writes to \texttt{out\_buff}. These are entirely independent operations,
so the loop is amenable to wide unrolling. The \texttt{UNROLL factor=64}
directive allows up to 64 iterations to execute in parallel, subject to
memory port availability, which is satisfied by the \texttt{cyclic
factor=4} partitioning applied to all three local buffers.

\subsection{C-Simulation Results}
\label{subsec:rmsnorm_csim}

C-simulation was performed by compiling \texttt{rmsnorm\_top} together
with a software testbench using GCC on the x86 host. The testbench
supplied a fixed eight-element input vector and a corresponding weight
vector, then compared the kernel output element by element against a
pure software reference implementation of RMSNorm. The reference
computes the sum of squares sequentially, derives the reciprocal RMS,
and applies the element-wise scaling in the same order as the
mathematical definition in \cref{subsec:rmsnorm}, with no HLS pragmas
or unrolling.

Table~\ref{tab:rmsnorm_csim} shows the output from both the kernel and
the software reference for each element of the test vector.

\begin{table}[H]
  \centering
  \begin{tabular}{rrrr}
    \toprule
    Index & HW output & SW reference & Absolute error \\
    \midrule
    0 &  0.099015 &  0.099015 & 0 \\
    1 & -0.396059 & -0.396059 & 0 \\
    2 &  0.891133 &  0.891133 & 0 \\
    3 & -1.584236 & -1.584236 & 0 \\
    4 &  0.495074 &  0.495074 & 0 \\
    5 & -1.188177 & -1.188177 & 0 \\
    6 &  2.079309 &  2.079309 & 0 \\
    7 & -3.168472 & -3.168472 & 0 \\
    \bottomrule
  \end{tabular}
  \caption{C-simulation output for \texttt{rmsnorm} with input
           $\mathbf{x} = [1, -2, 3, -4, 5, -6, 7, -8]$ and weight
           $\mathbf{g} = [0.5, 1.0, 1.5, 2.0, 0.5, 1.0, 1.5, 2.0]$.
           All eight elements match the software reference exactly.}
  \label{tab:rmsnorm_csim}

\end{table}

The absolute error is identically zero for every element, meaning the
kernel and the reference produce bit-identical results on this test
vector. This outcome is somewhat stricter than expected: because the
kernel's \texttt{sum\_of\_squares} loop uses \texttt{UNROLL factor=4},
HLS may group elements into sets of four and accumulate their squared
values in a different order from the sequential reference. Floating-point
addition is not associative, so reordering additions can produce
different rounding outcomes. In this particular case the zero-error result
reflects the small scale of the prototype dimensions ($S = 8$), where
the accumulated rounding differences are below the representational limit
of single-precision arithmetic. At the full TinyLlama scale ($S = 2048$),
small discrepancies between the unrolled and sequential summation orders
are expected, and a tolerance threshold rather than exact equality would
be the appropriate acceptance criterion. The csim run completed with zero
errors as reported by Vitis HLS.

\subsection{HLS Synthesis Results}
\label{subsec:rmsnorm_csyn}

The \texttt{rmsnorm} function was synthesised in isolation using the
prototype configuration ($S = 8$) on the \texttt{xczu9eg-ffvb1156-2-e}
device at a \SI{150}{\mega\hertz} target clock (\SI{6.667}{\nano\second}
period). Tables~\ref{tab:rmsnorm_timing} and \ref{tab:rmsnorm_top_latency}
reproduce the top-level timing and latency summary as reported by Vitis
HLS, followed by Table~\ref{tab:rmsnorm_resources} for resource utilisation
and Table~\ref{tab:rmsnorm_latency} for the per-stage latency breakdown.

\begin{table}[H]
  \centering
  \begin{tabular}{lrrr}
    \toprule
    Clock   & Target (ns) & Estimated (ns) & Uncertainty (ns) \\
    \midrule
    ap\_clk & 6.667       & 4.867          & 1.800 \\
    \bottomrule
  \end{tabular}
  \caption{Timing summary for \texttt{rmsnorm\_top} at the
           \SI{150}{\mega\hertz} (\SI{6.667}{\nano\second}) target clock.}
  \label{tab:rmsnorm_timing}

\end{table}

\begin{table}[H]
  \centering
  \begin{tabular}{rrrrrrl}
    \toprule
    \multicolumn{2}{c}{Latency (cycles)} &
    \multicolumn{2}{c}{Latency (absolute)} &
    \multicolumn{2}{c}{Interval (cycles)} & Pipeline \\
    min & max & min & max & min & max & type \\
    \midrule
    146 & 146 & \SI{0.973}{\micro\second} & \SI{0.973}{\micro\second} & 147 & 147 & no \\
    \bottomrule
  \end{tabular}
  \caption{Top-level latency summary for \texttt{rmsnorm\_top} at
           \SI{150}{\mega\hertz}.}
  \label{tab:rmsnorm_top_latency}

\end{table}

\begin{table}[H]
  \centering
  \begin{tabular}{lrrrr}
    \toprule
    Resource & Used (total) & Used (core only) & Available & Utilisation (\%) \\
    \midrule
    LUT      & 4{,}646 & 3{,}575 & 274{,}080 & 1 \\
    FF       & 3{,}910 & 2{,}949 & 548{,}160 & $<$1 \\
    BRAM     & 4       & 0       & 1{,}824   & $<$1 \\
    DSP      & 26      & 26      & 2{,}520   & 1 \\
    \bottomrule
  \end{tabular}
  \caption{Resource utilisation of the \texttt{rmsnorm} function
           synthesised at $S = 8$ on the ZCU102
           (\texttt{xczu9eg-ffvb1156-2-e}). Figures in the core-only
           column exclude the AXI master adapter overhead, which is
           not present when the function is inlined into the full kernel.}
  \label{tab:rmsnorm_resources}

\end{table}

\begin{table}[H]
  \centering
  \begin{tabular}{lrrrr}
    \toprule
    Stage & Trip count & II & Iteration latency & Total (cycles) \\
    \midrule
    Load \texttt{x} into \texttt{x\_buff}           & 8 &  1 &  3 &  9 \\
    \texttt{sum\_of\_squares}                        & 2 & 24 & 29 & 52 \\
    Load \texttt{weight} into \texttt{weight\_buff}  & 8 &  1 &  3 & 11 \\
    \texttt{norm\_and\_scale}                        & 8 &  1 &  2 & 10 \\
    \bottomrule
  \end{tabular}
  \caption{Latency breakdown of \texttt{rmsnorm} ($S = 8$) by internal
           pipeline stage at \SI{150}{\mega\hertz}. Total function latency
           is 144 cycles (\SI{960}{\nano\second}).}
  \label{tab:rmsnorm_latency}

\end{table}

The total function latency of 146 cycles is dominated by the
\texttt{sum\_of\_squares} reduction loop, which alone accounts for 52
of those cycles despite iterating only twice after the
\texttt{UNROLL factor=4} collapses the 8-element loop into 2 effective
iterations. The cause is the loop-carried dependency on the
floating-point accumulator \texttt{ss}. The Bind Op report shows that
HLS mapped the floating-point addition to a full-DSP \texttt{fadd} unit
with a pipeline latency of 5 cycles at this clock frequency. Because each
iteration must read the current value of \texttt{ss} and write back an
updated value, the next iteration cannot begin until the adder has
produced its result, giving an effective II of 24 cycles. The
\texttt{UNROLL factor=4} partially mitigates this by reducing the trip
count from 8 to 2, cutting the number of stall events in half, but it
cannot eliminate the dependency itself. A more complete solution would be
accumulator interleaving, where four independent partial sums are
maintained and merged only at the end, allowing all four lanes to pipeline
at II\,=\,1 independently. This is identified as a future optimisation
opportunity.

In contrast, the \texttt{norm\_and\_scale} loop achieves II\,=\,1
perfectly. Once the scalar reciprocal RMS \texttt{ss} is available, every
element's computation is independent of all others, allowing HLS to start
a new element every clock cycle. The \texttt{UNROLL factor=64} on an
8-element loop effectively fully unrolls it into 8 parallel datapath
lanes, which is reflected in the 26 DSPs consumed: 8 floating-point
multipliers for the \texttt{weight\,$\times$\,(ss\,$\times$\,x)}
computation each map to 3 DSPs, accounting for 24 of the 26 total, with
the remaining 2 used by the \texttt{fadd} in the reduction.

The 4 BRAMs in the total utilisation are consumed entirely by the AXI
master adapter (\texttt{gmem0\_m\_axi}) of the standalone wrapper and
are not a property of the \texttt{rmsnorm} logic itself. The
\texttt{rmsnorm} core uses no BRAM, since the local buffers
\texttt{x\_buff}, \texttt{weight\_buff}, and \texttt{out\_buff} at
$S = 8$ are small enough to be inferred as register files. The estimated
clock period of \SI{4.867}{\nano\second} leaves \SI{1.800}{\nano\second}
of uncertainty budget, which exactly meets the \SI{6.667}{\nano\second}
target with zero slack remaining. This is reflected in the \texttt{Timing}
issue flag visible in the synthesis summary report. While the design meets
the timing constraint, the zero-slack condition means there is no margin
for additional logic to be added along the critical path at this clock
frequency without risking a timing violation.

% =============================================================================
\section{Quantisation}
\label{sec:quantize_impl}
% =============================================================================

\subsection{Implementation}
\label{subsec:quantize_impl}

The \texttt{quantize<S>} function implements the group-wise symmetric
INT8 quantisation scheme described in \cref{sec:quantisation}. It is
declared as a C++ template in \texttt{forward.h}, parameterised by the
vector length \texttt{S}, and outputs results through the
\texttt{QuantizedTensor<S>} struct. A \texttt{QuantizedTensor<S>}
bundles an \texttt{int8\_t} array \texttt{q[S]} for the quantised values
and a \texttt{float} array \texttt{s[S]} for the scale factors, where
only \texttt{S/GS} scale entries are populated, one per group of
\texttt{GS} consecutive elements.

The \texttt{quantize<S>} function
processes the input float vector in groups of \texttt{GS} elements, as
shown in Listing~\ref{lst:quantize}. For each group, the function first
scans the \texttt{GS} elements to find the maximum absolute value
\texttt{wmax}, then computes the group scale as $s = \texttt{wmax} /
127.0$, which maps the largest magnitude in the group to the maximum
representable INT8 value of 127. Each element is then divided by the
scale and rounded to the nearest integer, producing a value in
$[-127, 127]$ that is stored as \texttt{int8\_t}. The quantised values
and scales are accumulated into local buffers
\texttt{quantized\_buffer[S]} and \texttt{scale\_buffer[num\_groups]}
before being written to the output struct via \texttt{memcpy}, avoiding
repeated writes to the output pointer during the inner loops.

\begin{lstlisting}[
  caption = {Group-wise quantisation in \texttt{quantize<S>} from
             \texttt{forward.h}. The outer loop iterates over groups;
             the inner scan finds \texttt{wmax} and the second inner loop
             converts each element. The compile-time constant \texttt{GS}
             replaces the runtime parameter \texttt{runtime\_gs} so the
             compiler can evaluate \texttt{S/GS} as a constant expression.},
  label   = {lst:quantize}
]
for (int group = 0; group < num_groups; group++) {
    #pragma HLS UNROLL factor=1
    #pragma HLS PIPELINE
    float wmax = 0.0f;
    int base_idx = group * GS;

    for (int i = 0; i < GS; i++) {          // find max abs value in group
        #pragma HLS PIPELINE
        float val = fabs(x[base_idx + i]);
        if (val > wmax) wmax = val;
    }

    float scale = wmax / 127.0f;             // symmetric INT8 scale
    scale_buffer[group] = scale;

    for (int i = 0; i < GS; i++) {          // quantise each element
        #pragma HLS PIPELINE
        quantized_buffer[base_idx + i] =
            (int8_t)round(x[base_idx + i] / scale);
    }
}
\end{lstlisting}

A notable implementation detail is the use of \texttt{GS} as a
compile-time constant from \texttt{config.h} rather than the
\texttt{runtime\_gs} parameter that is formally passed to the function.
The reason the parameter exists at all is to prevent the C++ compiler
from issuing a warning about a shadowed variable: an earlier iteration of
the code attempted to use a local variable also named \texttt{GS}, which
conflicted with the global constant. The rename to \texttt{runtime\_gs}
resolves the shadowing, and the function body then uses the global
\texttt{GS} constant directly. Because \texttt{GS} is a compile-time
constant, the compiler can statically evaluate \texttt{S/GS} as
\texttt{num\_groups}, enabling the \texttt{ARRAY\_PARTITION} pragma on
\texttt{quantized\_buffer} and allowing the inner loops to be analysed
at synthesis time without variable-bound ambiguity.

The \texttt{PIPELINE} pragma on the outer group loop combined with
\texttt{UNROLL factor=1} is an intentional configuration. Applying
\texttt{PIPELINE} to the outer loop while leaving the inner scan and
quantisation loops unpipelined would normally cause HLS to flatten the
inner loops into the outer pipeline, which at \texttt{GS=4} means four
sequential comparisons per outer iteration. The \texttt{UNROLL factor=1}
prevents the outer loop from being unrolled further, ensuring that the
group-boundary indexing remains predictable.

\subsection{C-Simulation Results}
\label{subsec:quantize_csim}

C-simulation was performed by compiling \texttt{quantize\_top} together
with a software testbench using GCC on the x86 host. The testbench supplied
an eight-element float vector containing a mix of positive and negative
values with varying magnitude and compared the kernel output against a
software reference implementation of the same group-wise symmetric INT8
quantisation scheme. Two aspects were verified independently: the INT8
quantised values and the per-group scale factors.

Table~\ref{tab:quantize_csim} shows the INT8 output and scale factors
produced by the kernel and the software reference for each element and
group respectively.

\begin{table}[H]
  \centering
  \begin{tabular}{lrrrr}
    \toprule
    Index & Input ($x$) & HW \texttt{q} & SW \texttt{q} & Match \\
    \midrule
    0 &  0.5 &   17 &   17 & \checkmark \\
    1 & -1.2 &  -41 &  -41 & \checkmark \\
    2 &  3.7 &  127 &  127 & \checkmark \\
    3 & -0.3 &  -10 &  -10 & \checkmark \\
    4 &  2.1 &   59 &   59 & \checkmark \\
    5 & -4.5 & -127 & -127 & \checkmark \\
    6 &  1.0 &   28 &   28 & \checkmark \\
    7 & -2.8 &  -79 &  -79 & \checkmark \\
    \midrule
    Group & & HW scale & SW scale & Abs error \\
    \midrule
    0 & & 0.029134 & 0.029134 & 0 \\
    1 & & 0.035433 & 0.035433 & 0 \\
    \bottomrule
  \end{tabular}
  \caption{C-simulation output for \texttt{quantize} with input
           $\mathbf{x} = [0.5, -1.2, 3.7, -0.3, 2.1, -4.5, 1.0, -2.8]$.
           All INT8 values and both scale factors match the software
           reference exactly. GS\,=\,4 so group~0 covers indices 0--3
           and group~1 covers indices 4--7.}
  \label{tab:quantize_csim}

\end{table}

All eight INT8 values and both scale factors agree exactly between the
hardware kernel and the software reference. Elements at index~2 and
index~5 map to the boundary values of $+127$ and $-127$ respectively,
confirming that the maximum-magnitude element in each group correctly
saturates the INT8 range. The scale for group~0 is $3.7 / 127 \approx
0.02913$, and for group~1 is $4.5 / 127 \approx 0.03543$, both of which
match the reference to the displayed precision. The csim run completed
with zero errors.

\subsection{HLS Synthesis Results}
\label{subsec:quantize_csyn}

The \texttt{quantize} function was synthesised in isolation at
\SI{150}{\mega\hertz} on the \texttt{xczu9eg-ffvb1156-2-e} device.
Tables~\ref{tab:quantize_timing} and \ref{tab:quantize_top_latency}
present the top-level timing and latency summary, followed by
Table~\ref{tab:quantize_resources} for resource utilisation and
Table~\ref{tab:quantize_latency} for the per-stage breakdown.

\begin{table}[H]
  \centering
  \begin{tabular}{lrrr}
    \toprule
    Clock & Target (ns) & Estimated (ns) & Uncertainty (ns) \\
    \midrule
    ap\_clk & 6.667 & 4.867 & 1.800 \\
    \bottomrule
  \end{tabular}
  \caption{Timing summary for \texttt{quantize\_top} at
           \SI{150}{\mega\hertz}.}
  \label{tab:quantize_timing}

\end{table}

\begin{table}[H]
  \centering
  \begin{tabular}{rrrrrrl}
    \toprule
    \multicolumn{2}{c}{Latency (cycles)} &
    \multicolumn{2}{c}{Latency (absolute)} &
    \multicolumn{2}{c}{Interval (cycles)} & Pipeline \\
    min & max & min & max & min & max & type \\
    \midrule
    84 & 84 & \SI{0.560}{\micro\second} & \SI{0.560}{\micro\second} & 85 & 85 & no \\
    \bottomrule
  \end{tabular}
  \caption{Top-level latency summary for \texttt{quantize\_top} at
           \SI{150}{\mega\hertz}.}
  \label{tab:quantize_top_latency}

\end{table}

\begin{table}[H]
  \centering
  \begin{tabular}{lrrrr}
    \toprule
    Resource & Used (total) & Used (core only) & Available & Utilisation (\%) \\
    \midrule
    LUT  & 6{,}379 & 3{,}275 & 274{,}080 & 2 \\
    FF   & 3{,}556 & 1{,}653 & 548{,}160 & $<$1 \\
    BRAM & 4       & 0       & 1{,}824   & $<$1 \\
    DSP  & 0       & 0       & 2{,}520   & 0 \\
    \bottomrule
  \end{tabular}
  \caption{Resource utilisation of \texttt{quantize} synthesised at
           $S = 8$ on the ZCU102. The core-only column excludes the
           AXI master adapter.}
  \label{tab:quantize_resources}

\end{table}

\begin{table}[H]
  \centering
  \begin{tabular}{lrrrr}
    \toprule
    Stage & Trip count & II & Iteration latency & Total (cycles) \\
    \midrule
    \texttt{main\_loop} (quantise core) & 2 & 4  & 41 & 44 \\
    Pipeline\_2 (write \texttt{q\_out}) & 8 & 1  &  1 &  8 \\
    Pipeline\_3 (write \texttt{s\_out}) & 2 & 1  &  1 &  2 \\
    \bottomrule
  \end{tabular}
  \caption{Latency breakdown of \texttt{quantize} ($S = 8$, $GS = 4$)
           by internal pipeline stage at \SI{150}{\mega\hertz}.
           Total function latency is 84 cycles
           (\SI{560}{\nano\second}).}
  \label{tab:quantize_latency}

\end{table}

The most striking result is the zero DSP utilisation. In contrast to
RMSNorm, which consumed 26 DSPs for its floating-point multiplications,
\texttt{quantize} uses none. The Bind Op report confirms that both
division operations: computing the group scale as $s =
\texttt{wmax}/127$ and quantising each element as $q_i = x_i / s$, are
mapped to fabric-based \texttt{fdiv} units with the \texttt{no\_dsp}
suffix. This occurs because Xilinx DSP48 blocks natively support only
multiply-accumulate operations; floating-point division has no direct DSP
mapping and must instead be implemented using LUT-based logic. The
consequence is higher LUT usage (3{,}275 core LUTs versus 3{,}575 for
RMSNorm) but zero DSP consumption, which is favourable since DSPs are the
most scarce resource on the ZCU102 relative to LUTs.

The \texttt{main\_loop} achieves II\,=\,4 across its two group
iterations, meaning a new group begins every 4 clock cycles. This is
determined by the inner loop over $GS = 4$ elements within each group:
since the inner quantisation loop is pipelined at II\,=\,1, processing
four elements takes four cycles and the outer group loop can start its
next iteration once those four cycles have elapsed. The iteration latency
of 41 cycles is dominated by the two \texttt{fdiv} units, each with an
11-cycle pipeline depth at \SI{150}{\mega\hertz}. Two \texttt{fdiv}
instances are synthesised — one for the scale computation and one for the
per-element quantisation divisions, allowing the two operations to
overlap in the pipeline. The intermediate \texttt{QuantizedTensor} struct
fields \texttt{qt.q} and \texttt{qt.s} are inferred as small register
files (\texttt{qt\_q\_U} and \texttt{qt\_s\_U}) rather than BRAMs, since
their sizes of 8 bytes and 32 bytes respectively fall well below the
BRAM threshold. The timing closure situation is identical to RMSNorm:
estimated \SI{4.867}{\nano\second} plus \SI{1.80}{\nano\second}
uncertainty exactly exhausts the \SI{6.667}{\nano\second} budget,
leaving zero slack.

% =============================================================================
\section{Dequantisation}
\label{sec:dequantize_impl}
% =============================================================================

\subsection{Implementation}
\label{subsec:dequantize_impl}

The \texttt{dequantize<S>} function is the inverse of
\texttt{quantize<S>}. For each element at index $i$, it multiplies the
INT8 value \texttt{qx->q[i]} by the corresponding group scale
\texttt{qx->s[i/GS]}, recovering an approximation of the original float.
The division \texttt{i/GS} computes the group index, and because
\texttt{GS} is a power of two, the compiler replaces this with a
bitwise right shift at no hardware cost. No pragma directives are
applied to \texttt{dequantize} in the prototype, since it operates on
a single vector element at a time with no loop-carried dependencies and
no memory partitioning decisions to make.

The maximum reconstruction error per element is bounded by
$\frac{1}{2} \cdot s = \frac{\texttt{wmax}}{254}$, which for typical
activation magnitudes of order 1.0 gives an absolute error of
approximately 0.004. This error is acceptable for the attention
projection inputs but motivates the design decision in the full
TinyLlama kernel to store KV cache values with per-group scales rather
than a single scale shared across the full \texttt{kv\_dim}-dimensional
vector.

\subsection{C-Simulation Results}
\label{subsec:dequantize_csim}

C-simulation was performed by compiling \texttt{dequantize\_top} together
with a software testbench using GCC on the x86 host. The testbench
supplied eight known INT8 values along with two explicit group scale
factors, one per \texttt{GS\,=\,4} element group, then compared the
kernel output element by element against a software reference computing
$x_{\text{ref}}[i] = q[i] \times s_{\lfloor i/GS \rfloor}$ directly.
The same group scale factors from the \texttt{quantize} csim run
($s_0 = 0.029134$, $s_1 = 0.035433$) were reused, making the two
testbenches complementary. Table~\ref{tab:dequantize_csim} shows the
kernel and reference outputs for each element.

\begin{table}[H]
  \centering
  \begin{tabular}{rrrr}
    \toprule
    Index & HW output & SW reference & Absolute error \\
    \midrule
    0 &  1.835442 &  1.835442 & 0 \\
    1 & -2.942534 & -2.942534 & 0 \\
    2 &  3.700018 &  3.700018 & 0 \\
    3 & -1.107092 & -1.107092 & 0 \\
    4 &  2.976372 &  2.976372 & 0 \\
    5 & -4.499991 & -4.499991 & 0 \\
    6 &  1.807083 &  1.807083 & 0 \\
    7 & -4.039362 & -4.039362 & 0 \\
    \bottomrule
  \end{tabular}
  \caption{C-simulation output for \texttt{dequantize} with group scales
           $s_0 = 0.029134$ (indices 0--3) and $s_1 = 0.035433$
           (indices 4--7). All eight dequantised float values match the
           software reference with zero absolute error.}
  \label{tab:dequantize_csim}

\end{table}

All eight dequantised float values agree exactly with the software
reference, with zero absolute error across every element. Elements at
indices 2 and 5, corresponding to the INT8 boundary values $+127$ and
$-127$, correctly reconstruct to $+3.700018$ and $-4.499991$
respectively, confirming that the scale multiplication path handles
boundary saturation without loss. The zero-error result is expected for
dequantisation: unlike quantisation, which introduces an irreducible
rounding error at the point of integer conversion, dequantisation is a
pure floating-point multiplication with no rounding decision, and the
kernel output is therefore bit-identical to the sequential software
reference on every element. The csim run completed with zero errors as
reported by Vitis HLS.

\subsection{HLS Synthesis Results}
\label{subsec:dequantize_csyn}

The \texttt{dequantize} function was synthesised in isolation under
identical conditions to \texttt{quantize}.
Tables~\ref{tab:dequantize_timing} and \ref{tab:dequantize_top_latency}
present the top-level timing and latency summary, followed by
Table~\ref{tab:dequantize_resources} for resource utilisation and
Table~\ref{tab:dequantize_latency} for the per-stage breakdown.

\begin{table}[H]
  \centering
  \begin{tabular}{lrrr}
    \toprule
    Clock & Target (ns) & Estimated (ns) & Uncertainty (ns) \\
    \midrule
    ap\_clk & 6.667 & 4.867 & 1.800 \\
    \bottomrule
  \end{tabular}
  \caption{Timing summary for \texttt{dequantize\_top} at
           \SI{150}{\mega\hertz}.}
  \label{tab:dequantize_timing}

\end{table}

\begin{table}[H]
  \centering
  \begin{tabular}{rrrrrrl}
    \toprule
    \multicolumn{2}{c}{Latency (cycles)} &
    \multicolumn{2}{c}{Latency (absolute)} &
    \multicolumn{2}{c}{Interval (cycles)} & Pipeline \\
    min & max & min & max & min & max & type \\
    \midrule
    45 & 45 & \SI{0.300}{\micro\second} & \SI{0.300}{\micro\second} & 46 & 46 & no \\
    \bottomrule
  \end{tabular}
  \caption{Top-level latency summary for \texttt{dequantize\_top} at
           \SI{150}{\mega\hertz}.}
  \label{tab:dequantize_top_latency}

\end{table}

\begin{table}[H]
  \centering
  \begin{tabular}{lrrrr}
    \toprule
    Resource & Used (total) & Used (core only) & Available & Utilisation (\%) \\
    \midrule
    LUT  & 3{,}128 &   273 & 274{,}080 & 1 \\
    FF   & 2{,}244 &   372 & 548{,}160 & $<$1 \\
    BRAM & 4       &     0 & 1{,}824   & $<$1 \\
    DSP  & 3       &     3 & 2{,}520   & $<$1 \\
    \bottomrule
  \end{tabular}
  \caption{Resource utilisation of \texttt{dequantize} synthesised at
           $S = 8$ on the ZCU102. The core-only column excludes the
           AXI master adapter.}
  \label{tab:dequantize_resources}

\end{table}

\begin{table}[H]
  \centering
  \begin{tabular}{lrrrr}
    \toprule
    Stage & Trip count & II & Iteration latency & Total (cycles) \\
    \midrule
    \texttt{VITIS\_LOOP\_55\_1} (dequantise loop) & 8 & 1 & 12 & 18 \\
    \bottomrule
  \end{tabular}
  \caption{Latency breakdown of \texttt{dequantize} ($S = 8$, $GS = 4$)
           at \SI{150}{\mega\hertz}. Total function latency is 45 cycles
           (\SI{300}{\nano\second}).}
  \label{tab:dequantize_latency}

\end{table}

The synthesis results reveal a significant asymmetry between the two
inverse operations. \texttt{Dequantize} is nearly twice as fast (45
cycles versus 84 cycles), consumes only a twelfth of the core LUTs
(273 versus 3{,}275), and achieves a perfect II\,=\,1 on its inner
loop. The root cause of this asymmetry is the difference between the
arithmetic operations each function requires.

\texttt{Dequantize} performs a single floating-point multiplication per
element, $x_i = q_i \times s_{\lfloor i/GS \rfloor}$, which maps to one
\texttt{fmul} unit consuming 3 DSP blocks with a 3-cycle pipeline latency.
Since each element's computation depends only on its own INT8 value and its
group scale with no dependency on any previous element, there are no loop-carried
dependencies, and II\,=\,1 is achievable. The 12-cycle iteration latency
reflects the \texttt{fmul} pipeline depth combined with the AXI read
latency for loading \texttt{q\_in} and \texttt{s\_in}.

\texttt{Quantize}, by contrast, requires two floating-point divisions per
group: one to compute $s = \texttt{wmax}/127$ and one per element for
$\hat{x}_i = x_i/s$. Unlike multiplication, floating-point division has
no mapping to Xilinx DSP48 blocks and is implemented entirely in LUT
fabric, as confirmed by the \texttt{no\_dsp} suffix on both
\texttt{fdiv} instances in the Bind Op report. This explains why
\texttt{quantize} consumes zero DSPs while using significantly more LUTs.
The 11-cycle \texttt{fdiv} latency at \SI{150}{\mega\hertz}, combined
with the sequential dependency between scale computation and element
quantisation within each group, is what drives the 41-cycle iteration
latency in \texttt{main\_loop}.

An additional artefact visible in the \texttt{quantize} Bind Storage
report is the presence of two small ROM lookup tables,
\texttt{mask\_table} and \texttt{one\_half\_table}, which HLS infers to
implement the IEEE~754 round-to-nearest operation used by the
\texttt{round()} call. These tables are inferred in register fabric
rather than BRAM at this prototype scale and contribute to the higher
LUT count. The AXI data width report for \texttt{dequantize} also shows
an interesting contrast: the \texttt{q\_in} port operates at an 8-bit
native width widened to a 32-bit AXI bus width (8\,$\rightarrow$\,32),
reflecting the fact that INT8 data must be packed into the 32-bit AXI
data bus in groups of four bytes per transfer.

% =============================================================================
\section{Matrix--Vector Multiplication}
\label{sec:matmul_impl}
% =============================================================================

\subsection{Implementation}
\label{subsec:matmul_impl}

The \texttt{matmul<N,D>} function computes the quantised matrix--vector
product $\mathbf{W}\mathbf{x} \to \mathbf{y}$, where $\mathbf{W} \in
\mathbb{Z}^{D \times N}$ is an INT8 weight matrix stored in row-major
order and $\mathbf{x} \in \mathbb{Z}^N$ is an INT8 input vector. Both
operands are accompanied by their group-wise scale factors, and the output
$\mathbf{y} \in \mathbb{R}^D$ is returned as a dequantised float vector.
The function is declared as a C++ template in \texttt{forward.h},
parameterised by \texttt{N} (input dimension) and \texttt{D} (output
dimension), and is instantiated at the call sites in \texttt{forward.cpp}
for the Q, K, V, and output projections.

The implementation is organised into three phases. The first phase
pre-loads the input vector and its associated scale factors into two
static local buffers before any row computation begins. The input buffer
\texttt{x\_buffer[N]} receives the INT8 input values via the
\texttt{x\_buff} loop with \texttt{UNROLL factor=16}, and the scale buffer
\texttt{xs\_buffer[N/GS]} receives the per-group float scales via the
\texttt{xs\_buff} loop with \texttt{UNROLL factor=4}. Both buffers are
declared \texttt{static} so that HLS allocates them as persistent on-chip
storage rather than on the call stack, and both are partitioned with
\texttt{ARRAY\_PARTITION cyclic} to provide sufficient read ports for the
downstream compute loops. Pre-loading into local buffers serves the same
purpose as in \texttt{rmsnorm}: it separates the AXI burst reads for the
input from the per-row AXI burst reads for the weights, preventing
contention on the shared \texttt{gmem0} port.

The second and third phases execute inside the outer row loop over
\texttt{D} output elements, which carries the \texttt{\#pragma HLS
PIPELINE} directive. For each row \texttt{i}, HLS allocates two
non-static local buffers, \texttt{w\_buffer[N]} and
\texttt{ws\_buffer[N/GS]}, each partitioned with \texttt{ARRAY\_PARTITION
cyclic factor=32}. The \texttt{matmul1} loop loads the \texttt{N} INT8
weight values for row \texttt{i} from DDR into \texttt{w\_buffer}, and
\texttt{matmul2} loads the corresponding \texttt{N/GS} scale factors into
\texttt{ws\_buffer}. Crucially, neither of these inner loading loops
carries an \texttt{UNROLL} pragma: the directives are present in the
source but commented out. This is a deliberate conservative design
decision for the prototype. Enabling \texttt{UNROLL} on \texttt{matmul1}
at full scale ($N = 2048$) would cause HLS to generate 2048 simultaneous
AXI read requests when the outer loop is pipelined, saturating the AXI
master adapter's 16-slot outstanding transaction queue and causing a
circular wait stall. The commented-out state reflects the minimum safe
configuration for prototype correctness.

The third phase is the grouped integer dot product. The \texttt{matmul3}
loop iterates over groups of \texttt{GS} consecutive elements, and within
each group the \texttt{matmul4} loop accumulates the INT8--INT8 products
into a 32-bit integer accumulator \texttt{ival}. Using \texttt{int32\_t}
for the accumulator is essential: the product of two INT8 values can reach
$127 \times 127 = 16{,}129$, and summing \texttt{GS = 4} such products
gives a maximum of $4 \times 16{,}129 = 64{,}516$, which fits within
\texttt{int16\_t} but would overflow \texttt{int8\_t}. Accumulating into
\texttt{int32\_t} prevents any possibility of overflow. After all
\texttt{GS} products have been accumulated, the group result is
dequantised inline: the integer sum is cast to float and multiplied by the
product of the weight group scale \texttt{ws\_buffer[j/GS]} and the input
group scale \texttt{xs\_buffer[j/GS]}, recovering the floating-point
partial sum for that group. This per-group dequantisation is added
directly to the running float accumulator \texttt{val} before moving to
the next group. Listing~\ref{lst:matmul} shows the core loop structure.

\begin{lstlisting}[
  caption = {Core loop structure of \texttt{matmul<N,D>} from
             \texttt{forward.h}. The outer row loop is pipelined; the
             inner loading loops (\texttt{matmul1}, \texttt{matmul2})
             and the grouped dot product loops (\texttt{matmul3},
             \texttt{matmul4}) execute sequentially within each pipeline
             iteration. The \texttt{UNROLL} pragmas on \texttt{matmul1}
             and \texttt{matmul2} are commented out to prevent AXI
             interface flooding at full scale.},
  label = {lst:matmul}
]
for (i = 0; i < D; i++) {
  #pragma HLS PIPELINE
  float val = 0.0f;
  int8_t  w_buffer[N];   float ws_buffer[N/GS];
  #pragma HLS ARRAY_PARTITION variable=w_buffer  cyclic factor=32
  #pragma HLS ARRAY_PARTITION variable=ws_buffer cyclic factor=32

matmul1: for (int j = 0; j < N; j++)   // no UNROLL: deliberate
    w_buffer[j]  = wq[j + i*N];
matmul2: for (int j = 0; j < N/GS; j++) // no UNROLL: deliberate
    ws_buffer[j] = ws[i*(N/GS) + j];

matmul3: for (int j = 0; j <= N-GS; j += GS) {
    int32_t ival = 0;
matmul4:  for (int k = 0; k < GS; k++) // UNROLL commented out
        ival += (int32_t)x_buffer[j+k] * (int32_t)w_buffer[j+k];
    val += (float)ival * ws_buffer[j/GS] * xs_buffer[j/GS];
  }
  xout[i] = val;
}
\end{lstlisting}

\subsection{C-Simulation Results}
\label{subsec:matmul_csim}

C-simulation was performed by compiling \texttt{matmul\_top} together
with a software testbench using GCC on the x86 host. The testbench
constructs an $8 \times 8$ weight matrix whose diagonal entries are all
127 and whose off-diagonal entries are all zero, with all weight scales
set to $1/127$. With this configuration the computation simplifies to
$\texttt{xout}[i] = \texttt{xq}[i] \times \texttt{xs}[\lfloor i/GS
\rfloor]$, which is precisely the dequantised input vector. The testbench
reuses the quantised values and scale factors from the \texttt{quantize}
csim as input, making the three testbenches a matched chain in which
quantise, identity matrix--vector multiply, and dequantise can be
cross-validated against one another.

Table~\ref{tab:matmul_csim} shows the output from both the kernel and the
software reference for each of the eight output elements.

\begin{table}[H]
  \centering
  \begin{tabular}{rrrr}
    \toprule
    Index & HW output & SW reference & Absolute error \\
    \midrule
    0 &  0.495278 &  0.495278 & 0 \\
    1 & -1.194494 & -1.194494 & 0 \\
    2 &  3.700018 &  3.700018 & 0 \\
    3 & -0.291340 & -0.291340 & 0 \\
    4 &  2.090547 &  2.090547 & 0 \\
    5 & -4.499991 & -4.499991 & 0 \\
    6 &  0.992124 &  0.992124 & 0 \\
    7 & -2.799207 & -2.799207 & 0 \\
    \bottomrule
  \end{tabular}
  \caption{C-simulation output for \texttt{matmul} with quantised input
           $\hat{\mathbf{x}} = [17, -41, 127, -10, 59, -127, 28, -79]$
           (group scales $s_0 = 0.029134$, $s_1 = 0.035433$) and an
           $8 \times 8$ scaled identity weight matrix. All eight output
           elements match the software reference within tolerance.}
  \label{tab:matmul_csim}

\end{table}

All eight output elements agree exactly between the hardware kernel and
the software reference. The expected round-trip relationship holds: the
output values are the dequantised equivalents of the original float inputs
$[0.5, -1.2, 3.7, -0.3, 2.1, -4.5, 1.0, -2.8]$ to within the precision
limits of symmetric INT8 quantisation, with the largest absolute
reconstruction error occurring at index~2 where the original value of
$3.7$ reconstructs to $3.700018$, a difference of $18 \times 10^{-6}$
arising from the scale factor $s_0 = 3.7/127 \approx 0.029134$ stored as
a 32-bit float. The csim run completed with zero errors as reported by
Vitis HLS.

\subsection{HLS Synthesis Results}
\label{subsec:matmul_csyn}

The \texttt{matmul} function was synthesised in isolation using the
prototype configuration ($N = D = 8$, $GS = 4$) on the
\texttt{xczu9eg-ffvb1156-2-e} device at a \SI{150}{\mega\hertz} target
clock (\SI{6.667}{\nano\second} period).
Tables~\ref{tab:matmul_timing} and \ref{tab:matmul_top_latency} present
the top-level timing and latency summary, followed by
Table~\ref{tab:matmul_resources} for resource utilisation and
Table~\ref{tab:matmul_latency} for the per-stage latency breakdown.

\begin{table}[H]
  \centering
  \begin{tabular}{lrrr}
    \toprule
    Clock   & Target (ns) & Estimated (ns) & Uncertainty (ns) \\
    \midrule
    ap\_clk & 6.667       & 4.867          & 1.800 \\
    \bottomrule
  \end{tabular}
  \caption{Timing summary for \texttt{matmul\_top} at the
           \SI{150}{\mega\hertz} (\SI{6.667}{\nano\second}) target clock.}
  \label{tab:matmul_timing}

\end{table}

\begin{table}[H]
  \centering
  \begin{tabular}{rrrrrrl}
    \toprule
    \multicolumn{2}{c}{Latency (cycles)} &
    \multicolumn{2}{c}{Latency (absolute)} &
    \multicolumn{2}{c}{Interval (cycles)} & Pipeline \\
    min & max & min & max & min & max & type \\
    \midrule
    146 & 146 & \SI{0.973}{\micro\second} & \SI{0.973}{\micro\second} & 147 & 147 & no \\
    \bottomrule
  \end{tabular}
  \caption{Top-level latency summary for \texttt{matmul\_top} at
           \SI{150}{\mega\hertz}.}
  \label{tab:matmul_top_latency}

\end{table}

\begin{table}[H]
  \centering
  \begin{tabular}{lrrrr}
    \toprule
    Resource & Used (total) & Used (core only) & Available & Utilisation (\%) \\
    \midrule
    LUT  & 5{,}577 & 2{,}506 & 274{,}080 & 2 \\
    FF   & 4{,}163 & 1{,}944 & 548{,}160 & $<$1 \\
    BRAM & 4       & 0       & 1{,}824   & $<$1 \\
    DSP  & 9       & 9       & 2{,}520   & $<$1 \\
    \bottomrule
  \end{tabular}
  \caption{Resource utilisation of \texttt{matmul} synthesised at $N = D
           = 8$ on the ZCU102 (\texttt{xczu9eg-ffvb1156-2-e}). The
           core-only column excludes the AXI master adapter and S-AXILite
           control interface, which are not present when the function is
           inlined into the full kernel.}
  \label{tab:matmul_resources}

\end{table}

\begin{table}[H]
  \centering
  \begin{tabular}{lrrrr}
    \toprule
    Stage & Trip count & II & Iteration latency & Total (cycles) \\
    \midrule
    Pre-loop buffer loads (\texttt{x\_buff}, \texttt{xs\_buff}) & — & — & — & 24 \\
    \texttt{VITIS\_LOOP\_143\_1} (row loop)                     & 8 & 10 & 42 & 120 \\
    \bottomrule
  \end{tabular}
  \caption{Latency breakdown of \texttt{matmul} ($N = D = 8$, $GS = 4$)
           at \SI{150}{\mega\hertz}. Total function latency is 146 cycles
           (\SI{973}{\nano\second}).}
  \label{tab:matmul_latency}

\end{table}

The central result is that the row loop \texttt{VITIS\_LOOP\_143\_1}
achieves an II of 10 against a target of 1. Understanding why requires
examining what the pipeline must accomplish in each iteration: before any
arithmetic can begin on row~\texttt{i}, the pipeline must first issue AXI
reads for the \texttt{N = 8} INT8 weight values from \texttt{matmul1} and
the \texttt{N/GS = 2} float scale values from \texttt{matmul2}. Because
neither loading loop carries an \texttt{UNROLL} pragma, HLS issues these
reads sequentially: eight weight reads followed by two scale reads, giving
$8 + 2 = 10$ sequential AXI transactions before the first arithmetic
operation of the iteration can complete. This sequential load count
directly determines the achieved II of 10. If the \texttt{UNROLL} pragmas
were enabled, HLS could issue all ten reads simultaneously, reducing II
toward~1, but doing so at full scale ($N = 2048$) would generate 2048
concurrent read requests that would saturate the adapter's outstanding
transaction limit and cause a deadlock stall, as detailed in
\cref{subsec:forward_limitations}.

The iteration latency of 42 cycles reflects the time required to complete
the full body of one row iteration: the sequential weight and scale loads
followed by the grouped integer dot product across two groups of four
elements each, culminating in the per-group floating-point dequantisation.
The total row loop latency is therefore $\text{II} \times (D - 1) +
\text{iteration latency} = 10 \times 7 + 42 = 112$ cycles for the
arithmetic, with the remaining 8 cycles accounting for pipeline drain and
sub-module handshaking overhead. The 24-cycle pre-loop overhead covers the
AXI address setup and the two buffer-loading loops for the input vector
and its scales, which execute once before any row computation begins.

The DSP utilisation of 9 breaks down as follows. The Bind Op report
identifies four \texttt{mac\_muladd\_8s\_8s\_16s\_17} units (one per INT8
element of the \texttt{matmul4} inner loop within one group, consuming 1
DSP each), one \texttt{fmul\_32ns} unit for the floating-point
dequantisation product $\texttt{ival} \times \texttt{ws} \times
\texttt{xs}$ (3 DSPs), and one \texttt{fadd\_32ns} unit for the
float accumulator \texttt{val} (2 DSPs), totalling $4 + 3 + 2 = 9$. An
additional four \texttt{mul\_8s\_8s\_16} instances appear in the report
with zero DSP assignment; these correspond to the second group's INT8
multiplications, which HLS chose to implement in LUT fabric rather than
DSP48 blocks. The asymmetric mapping, with one group in DSPs and the other
in LUTs, reflects HLS's resource allocation heuristics when the inner
\texttt{UNROLL} pragma is absent: without a directive forcing all four
elements of both groups to be instantiated in parallel, the tool schedules
one group's products through the DSP-backed MAC pipeline and places the
second group's products in available LUT fabric. The total DSP count of 9
is notably higher than the zero DSPs consumed by \texttt{quantize} and the
3 DSPs consumed by \texttt{dequantize}, reflecting that \texttt{matmul} is
the first primitive that performs both integer and floating-point
arithmetic requiring DSP48 resources for both. The timing situation is
identical to all preceding sections: estimated \SI{4.867}{\nano\second}
plus \SI{1.800}{\nano\second} uncertainty exactly exhausts the
\SI{6.667}{\nano\second} budget, leaving zero slack.

% =============================================================================
\section{Softmax}
\label{sec:softmax_impl}
% =============================================================================

\subsection{Implementation}
\label{subsec:softmax_impl}

% Describe softmax<MAXSIZE> from forward.h.
% Cover: numerically stable max-subtract form, four sequential loops
% (max, exp, sum, norm), loop_tripcount hints for variable-length
% operation, UNROLL factor=16 on exp and norm loops, inv_sum optimisation.
% Note the template parameter change from <16> to <257> in forward_fyp.h
% and explain why (accommodates larger seq_len without code changes).

\subsection{C-Simulation Results}
\label{subsec:softmax_csim}

\subsection{HLS Synthesis Results}
\label{subsec:softmax_csyn}

% =============================================================================
\section{Grouped-Query Attention}
\label{sec:attn_impl}
% =============================================================================

\subsection{Baseline: Floating-Point Attention}
\label{subsec:attn_fp32}

% Describe the baseline attention in forward.cpp:
% - QKV projections via matmul
% - Float KV cache write via memcpy
% - Per-head score computation with PIPELINE on outer t-loop
%   and UNROLL on inner head_size dot product
% - softmax<16> call
% - Value accumulation with same outer-PIPELINE structure
% - Note that PIPELINE on outer t-loop is safe at seq_len=16 but
%   will cause deadlock at full scale (forward reference to Sec 4.7)

\subsection{Refined: Per-Head INT8 Attention}
\label{subsec:attn_int8}

% Describe the per-head quantisation approach in forward_fyp.cpp.
% Cover: outlier suppression motivation, three-step structure
% (quantise Q per head, load+quantise K on-the-fly, INT32 dot product,
% dequantise score), value accumulation with prob_int8 and v_int8,
% xb_accum with ARRAY_PARTITION complete (register file).
% Reference eq:int8_score and eq:int8_value_acc.

\subsection{C-Simulation Results}
\label{subsec:attn_csim}

\subsection{HLS Synthesis Results}
\label{subsec:attn_csyn}

% =============================================================================
\section{SwiGLU Feed-Forward Network}
\label{sec:ffn_impl}
% =============================================================================

% The FFN is commented out in the prototype kernel. This section will be
% written once the full TinyLlama kernel files (FFN_update2/update3) are
% incorporated. Content will cover:
% - Three-matrix SwiGLU structure (w1 gate, w3 up, w2 down)
% - Swish activation: SiLU approximation via sigmoid LUT (I-BERT strategy)
% - Element-wise gating and down projection
% - UNROLL factor=16 on gate loop
% - hb and hb2 buffer partitioning

\subsection{Implementation}
\label{subsec:ffn_impl_detail}

\subsection{C-Simulation Results}
\label{subsec:ffn_csim}

\subsection{HLS Synthesis Results}
\label{subsec:ffn_csyn}

% =============================================================================
\section{Top-Level Forward Function}
\label{sec:forward_impl}
% =============================================================================

\subsection{AXI Interface and Buffer Layout}
\label{subsec:forward_axi}

% Describe the AXI bundle configuration:
% - Prototype: 2 bundles (gmem0: transformer+key_cache+value_cache, gmem1: out)
% - Full model: 4 bundles (gmem0: weights, gmem1: key, gmem2: value, gmem3: out)
% Explain why sharing caches on gmem0 with weights is acceptable at small
% scale but causes deadlock at full scale.
% Cover static buffer declarations and ARRAY_PARTITION annotations.

\subsection{Layer Loop and KV Cache Management}
\label{subsec:forward_layers}

% Describe the main_forward_loop over L layers:
% - Embedding lookup (float table in prototype)
% - RMSNorm -> quantize -> QKV matmuls
% - KV cache write (float in prototype, INT8 in full model)
% - Multihead attention loop
% - Residual addition
% Note that FFN and final RMSNorm are commented out in the prototype.

\subsection{Known Limitations and Design Decisions}
\label{subsec:forward_limitations}

% Cover the three deliberate simplifications in the prototype:
% 1. Output projection W_O disabled (xb used directly in residual)
% 2. FFN commented out (attention-only forward pass)
% 3. Float KV cache (INT8 cache added in full model)
% Frame these as conscious scope decisions, not bugs.
