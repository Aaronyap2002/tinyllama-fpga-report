\documentclass{article}

\usepackage{listings}
\usepackage{xcolor}
\usepackage{inconsolata}

\definecolor{codekeyword}{RGB}{30, 100, 200}
\definecolor{codecomment}{RGB}{0, 128, 96}
\definecolor{linehighlight}{RGB}{225, 225, 225}

\lstset{
  language=C++,
  basicstyle=\ttfamily\small,
  keywordstyle=\color{codekeyword}\bfseries,
  commentstyle=\color{codecomment}\itshape,
  morekeywords={pragma, HLS, PIPELINE, UNROLL, ARRAY_PARTITION,
                II, factor, cyclic, complete, dataflow},
  numbers=left,
  numberstyle=\tiny\color{gray},
  stepnumber=1,
  numbersep=8pt,
  frame=tb,           % only top and bottom lines, no side borders
  rulecolor=\color{gray!50},
  showstringspaces=false,
  breaklines=true,
  tabsize=2,
  captionpos=b,
}

\begin{document}

\title{Test Document}
\author{Aaron}
\date{\today}
\maketitle

Hello, this is a test. Here is some inline math: $E = mc^2$.

\begin{equation}
  \sum_{i=0}^{N} x_i \cdot w_i
\end{equation}

\begin{lstlisting}[
  caption={Pipelined dot product loop with HLS pragma.},
  label={lst:dot_product}
]
for (int i = 0; i < N; i++) {
    #pragma HLS PIPELINE II=1
    result += x[i] * w[i];  // accumulate weighted sum
}
\end{lstlisting}

\end{document}
